# ADR 002: Fallback is a first-class output

## Decision

Paradigm evaluates a reflex as a selective predictor, not a classifier forced to answer every input.

## Reason

The intended system uses deliberation for unfamiliar cases. Coverage, selective accuracy, and OOD false acceptance are therefore core metrics.
